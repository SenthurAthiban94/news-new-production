{
  "name": "news",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-cron": "^1.2.1",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4",
    "serve": "^6.5.6"
  },
  "scripts": {
    "start": "serve -s build",      //react-scripts start
    "build": "serve -s build", //react-scripts build
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
